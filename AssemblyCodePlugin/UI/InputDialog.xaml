<Window x:Class="AssemblyCodePlugin.UI.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Название конфигурации"
        Width="400" Height="190"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F8FAFC"
        FontFamily="Segoe UI, Segoe UI Emoji">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtPrompt" Grid.Row="0" Text="Введите имя конфигурации:" FontWeight="SemiBold" FontSize="13" Foreground="#0F172A" Margin="0,0,0,8"/>
        <TextBox x:Name="TxtInput" Grid.Row="1" FontSize="13" Padding="6,4"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="ОК" Width="80" Height="30" Margin="0,0,8,0" IsDefault="True" Click="BtnOk_Click"/>
            <Button Content="Отмена" Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
